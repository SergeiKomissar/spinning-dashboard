# 🏭 Дашборд прядильного цеха

Информационная панель для анализа качества термообработанной нити с данными из Google Sheets.

## Возможности

- **Мониторинг качества** — отслеживание разрывной нагрузки, коэффициента вариации и линейной плотности
- **Gauge-индикаторы** — визуальное отображение показателей качества
- **Динамика по партиям** — графики трендов за последние 10 партий
- **Карта качества** — scatter-диаграмма для анализа машин
- **Топ проблемных машин** — выявление машин с наибольшим количеством отклонений
- **Детальный анализ** — разворачиваемые карточки по каждой машине
- **Авторизация** — защита паролем с разделением на пользователей и админов
- **Статистика посещений** — для администраторов

## Требования

- Python 3.10+
- macOS (для автозапуска) или Linux
- Аккаунт Google Cloud с доступом к Sheets API

## Структура проекта

```
Dashboard/
├── app/
│   ├── dashboard.py          # Главный файл приложения
│   ├── components/           # Компоненты UI (графики, метрики)
│   ├── utils/                # Утилиты (авторизация, загрузка данных)
│   └── pages/                # Дополнительные страницы (админка)
├── config/
│   └── users.yaml            # Логины и пароли пользователей
├── credentials.json          # Ключ Google Sheets API (не в репозитории!)
├── requirements.txt          # Python-зависимости
├── start_server.sh           # Скрипт запуска
├── setup_autostart.sh        # Настройка автозапуска на Mac
├── com.spinning.dashboard.plist    # LaunchAgent для дашборда
└── com.cloudflare.tunnel.plist     # LaunchAgent для туннеля
```

## Установка

### 1. Клонирование и настройка окружения

```bash
cd ~/DashboardPVV/Dashboard
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 2. Настройка Google Sheets API

1. Создайте проект в [Google Cloud Console](https://console.cloud.google.com/)
2. Включите **Google Sheets API**
3. Создайте **сервисный аккаунт** и скачайте JSON-ключ
4. Переименуйте ключ в `credentials.json` и поместите в папку `Dashboard/`
5. Откройте вашу Google-таблицу и дайте доступ (Viewer) email'у сервисного аккаунта

### 3. Настройка пользователей

Отредактируйте `config/users.yaml`:

```yaml
users:
  admin:
    password: "ваш_пароль"
    name: "Имя Админа"
    role: "admin"
  user1:
    password: "пароль_пользователя"
    name: "Имя Пользователя"
    role: "user"
```

## Запуск

### Ручной запуск

```bash
cd ~/DashboardPVV/Dashboard
source venv/bin/activate
streamlit run app/dashboard.py --server.port 8501
```

Или через скрипт:

```bash
./start_server.sh
```

Дашборд будет доступен по адресу: **http://localhost:8501**

### Автозапуск при включении Mac

Дашборд и туннель уже настроены на автозапуск. Управление:

| Действие | Команда |
|----------|---------|
| Остановить дашборд | `launchctl unload ~/Library/LaunchAgents/com.spinning.dashboard.plist` |
| Запустить дашборд | `launchctl load ~/Library/LaunchAgents/com.spinning.dashboard.plist` |
| Остановить туннель | `launchctl unload ~/Library/LaunchAgents/com.cloudflare.tunnel.plist` |
| Запустить туннель | `launchctl load ~/Library/LaunchAgents/com.cloudflare.tunnel.plist` |
| Статус сервисов | `launchctl list \| grep -E "dashboard\|cloudflare"` |

### Логи

```bash
# Логи дашборда
cat /tmp/dashboard.log
cat /tmp/dashboard_error.log

# Логи туннеля (включая URL)
cat /tmp/cloudflare_tunnel_error.log
```

## Публичный доступ (Cloudflare Tunnel)

Туннель создаёт публичный URL для доступа к дашборду извне.

**Узнать текущий URL:**

```bash
cat /tmp/cloudflare_tunnel_error.log | grep trycloudflare
```

URL меняется при каждом перезапуске туннеля.

**Ручной запуск туннеля:**

```bash
cloudflared tunnel --url http://localhost:8501
```

## Полезные команды

```bash
# Проверить статус сервисов
launchctl list | grep -E "dashboard|cloudflare"

# Перезапустить всё
launchctl unload ~/Library/LaunchAgents/com.spinning.dashboard.plist
launchctl unload ~/Library/LaunchAgents/com.cloudflare.tunnel.plist
launchctl load ~/Library/LaunchAgents/com.spinning.dashboard.plist
launchctl load ~/Library/LaunchAgents/com.cloudflare.tunnel.plist

# Обновить Streamlit
source venv/bin/activate
pip install --upgrade streamlit

# Пересоздать виртуальное окружение (если проблемы)
rm -rf venv
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Устранение неполадок

### Дашборд не запускается

1. Проверьте логи: `cat /tmp/dashboard_error.log`
2. Убедитесь, что порт 8501 свободен: `lsof -i :8501`
3. Проверьте статус: `launchctl list | grep dashboard`
   - Код `0` = работает
   - Код `78` или `127` = ошибка (смотрите логи)

### Туннель не работает

1. Проверьте логи: `cat /tmp/cloudflare_tunnel_error.log`
2. Убедитесь, что cloudflared установлен: `which cloudflared`
3. Проверьте статус: `launchctl list | grep cloudflare`

### Ошибка "page_link() is not a valid Streamlit command"

Обновите Streamlit до версии 1.31.0+:

```bash
source venv/bin/activate
pip install --upgrade streamlit
```
