# 🏭 Дашборд прядильного цеха

## Описание

Веб-приложение для мониторинга качества термообработанной нити в прядильном цехе. Отображает ключевые метрики качества: разрывную нагрузку, коэффициент вариации, линейную плотность.

---

## 🌐 Где работает приложение

| Платформа | URL | Описание |
|-----------|-----|----------|
| **Streamlit Cloud** | https://spinning-dashboard-hzudfzmoxdjstvappwgtere.streamlit.app | ✅ Основной адрес (работает 24/7) |
| **GitHub** | https://github.com/SergeiKomissar/spinning-dashboard | Исходный код |

---

## 🔧 Технологии

- **Python 3.11** — язык программирования
- **Streamlit** — веб-фреймворк для дашбордов
- **Plotly** — интерактивные графики
- **Google Sheets API** — источник данных
- **Streamlit Cloud** — бесплатный облачный хостинг

---

## 📊 Функционал

1. **Gauge-индикаторы** — три показателя качества текущей партии
2. **График тренда** — динамика разрывной нагрузки по последним 10 партиям
3. **Топ проблемных машин** — 5 машин с наибольшим количеством отклонений
4. **Карта качества** — scatter-график (сворачиваемый)
5. **Таблица машин** — детализация по каждой машине с пагинацией (по 10 шт)
6. **Авторизация** — вход по логину/паролю
7. **Статистика посещений** — для администратора

---

## 👥 Пользователи

| Логин | Имя | Роль |
|-------|-----|------|
| sergeikomissarov | Сергей Комиссаров | Админ |
| ivangolotin | Иван Голотин | Пользователь |
| sergeipivovarov | Сергей Пивоваров | Пользователь |
| alexanderzlobin | Александр Злобин | Пользователь |

---

## 📁 Структура проекта

```
Dashboard/
├── app/
│   ├── dashboard.py          # Главная страница
│   ├── pages/
│   │   └── 1_admin_stats.py  # Статистика посещений (только для админа)
│   ├── components/
│   │   ├── charts.py         # Графики (Plotly)
│   │   ├── layout.py         # Стили CSS и layout
│   │   └── metrics.py        # Расчёт метрик
│   └── utils/
│       ├── auth.py           # Авторизация пользователей
│       ├── constants.py      # Константы (цвета, пороги)
│       └── data_processing.py # Загрузка данных из Google Sheets
├── .streamlit/
│   └── config.toml           # Настройки Streamlit (тема, сервер)
├── requirements.txt          # Зависимости Python
└── README.md                 # Этот файл
```

---

## 🔄 КАК ОБНОВИТЬ ПРИЛОЖЕНИЕ

### Изменить код:

1. Откройте **Терминал** на Mac
2. Выполните команды:
```bash
cd /Users/komissarov/DashboardPVV/Dashboard
# ... внесите изменения в файлы ...
git add .
git commit -m "Описание что изменили"
git push
```
3. **Готово!** Streamlit Cloud обновится автоматически через 1-2 минуты

### Изменить пользователей или пароли:

1. Откройте https://share.streamlit.io
2. Найдите приложение → нажмите **"⋮"** (три точки) → **"Settings"**
3. Перейдите во вкладку **"Secrets"**
4. Найдите нужного пользователя и измените данные:
```toml
[users.credentials.usernames.ivangolotin]
name = "Иван Голотин"
password = "новый_пароль"
role = "user"
```
5. Нажмите **"Save"**

### Добавить нового пользователя:

В секции Secrets добавьте блок:
```toml
[users.credentials.usernames.новый_логин]
name = "Имя Фамилия"
password = "пароль"
role = "user"
```

### Изменить Google-таблицу:

Данные берутся из Google Sheets по ключу в секции `[gcp_service_account]` в Secrets.

---

## 🛠 Локальный запуск (для разработки)

```bash
cd /Users/komissarov/DashboardPVV/Dashboard
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
streamlit run app/dashboard.py
```

Дашборд откроется на http://localhost:8501

---

## 🧹 Очистка папки

Удалите ненужные файлы (в терминале):
```bash
cd /Users/komissarov/DashboardPVV/Dashboard
rm -rf .venv venv .DS_Store
rm -f run.bat run.command run.sh start_server.sh
rm -f install_server.sh setup_autostart.sh
rm -f com.*.plist
rm -f DEPLOY_GUIDE.md STREAMLIT_CLOUD_DEPLOY.md PASSWORDS.txt
```

---

## 📞 Контакты

По вопросам обращайтесь к администратору: **Сергей Комиссаров**
