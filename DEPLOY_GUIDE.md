# 🚀 Инструкция по развёртыванию дашборда

## Пользователи системы

| Логин | Пароль | Роль |
|-------|--------|------|
| sergeikomissarov | 291977 | admin |
| ivangoloti | 847362 | user |
| sergeipivovarov | 529164 | user |
| alexanderzlobin | 673918 | user |

---

## Этап 1: Перенос на сервер (MacBook)

### 1.1 Скопируйте папку Dashboard на сервер

```bash
# Переименуйте папку в Dashboard и поместите в домашнюю директорию
mv "Dashboard — копия" ~/Dashboard
cd ~/Dashboard
```

### 1.2 Установите зависимости

```bash
./install_server.sh
```

### 1.3 Проверьте работу локально

```bash
./start_server.sh
```

Откройте в браузере: http://localhost:8501

---

## Этап 2: Настройка автозапуска

```bash
./setup_autostart.sh
```

Теперь дашборд будет запускаться автоматически при включении Mac.

### Команды управления:

```bash
# Посмотреть статус
launchctl list | grep spinning

# Остановить
launchctl unload ~/Library/LaunchAgents/com.spinning.dashboard.plist

# Запустить
launchctl load ~/Library/LaunchAgents/com.spinning.dashboard.plist

# Посмотреть логи
tail -f /tmp/dashboard.log
```

---

## Этап 3: Доступ из интернета (Cloudflare Tunnel)

### 3.1 Установите cloudflared

```bash
brew install cloudflare/cloudflare/cloudflared
```

### 3.2 Авторизуйтесь в Cloudflare

```bash
cloudflared tunnel login
```

### 3.3 Создайте туннель

```bash
cloudflared tunnel create spinning-dashboard
```

### 3.4 Запустите туннель

```bash
cloudflared tunnel --url http://localhost:8501
```

Вы получите ссылку вида: `https://xxxxx.cfargotunnel.com`

### 3.5 Для постоянной работы туннеля

```bash
# Создайте конфиг
mkdir -p ~/.cloudflared
cat > ~/.cloudflared/config.yml << CONFIG
tunnel: spinning-dashboard
credentials-file: ~/.cloudflared/<ID>.json
ingress:
  - service: http://localhost:8501
CONFIG

# Установите как сервис
cloudflared service install
```

---

## Этап 4: Настройка Mac для работы 24/7

### 4.1 Отключите засыпание

**Системные настройки → Экономия энергии:**
- "Переводить диски в режим сна" — выключить
- "Просыпаться для доступа к сети" — включить

Или через терминал:
```bash
sudo pmset -a disablesleep 1
sudo pmset -a sleep 0
```

### 4.2 Автовход при включении

**Системные настройки → Пользователи → Параметры входа:**
- "Автоматический вход" — выберите вашего пользователя

---

## Управление пользователями

### Добавить нового пользователя

Отредактируйте файл `config/users.yaml`:

```yaml
credentials:
  usernames:
    newuser:
      name: Имя Фамилия
      password: '123456'
      role: user  # или admin
```

### Изменить пароль

Измените значение `password` в `config/users.yaml`

---

## Статистика посещений

Администратор может посмотреть статистику:
1. Войдите под учёткой sergeikomissarov
2. В боковом меню нажмите "📊 Статистика посещений"

Данные хранятся в `data/visits.db`

---

## Решение проблем

### Дашборд не запускается

```bash
# Проверьте логи
cat /tmp/dashboard_error.log

# Проверьте порт
lsof -i :8501
```

### Ошибка Google Sheets

Проверьте что `credentials.json` на месте и имеет правильные права доступа к таблице.

### Сброс сессий

```bash
rm -rf data/visits.db
```

---

## Контакты

При возникновении проблем обратитесь к администратору системы.
